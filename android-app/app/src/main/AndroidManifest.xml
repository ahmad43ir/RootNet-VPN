<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Saving downloads to the public Downloads folder on Android 9 and
         older (API 28-) needs WRITE_EXTERNAL_STORAGE. API 29+ uses
         MediaStore.Downloads instead, which needs no permission. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Adivery only (v2.2): its AAR merges its own permissions/activities. -->
    <application
        android:name=".RootNetApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RootNet">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.RootNet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- v2.1.1: appear in Android's "Open with" chooser for VPN config
                 files (.npvt / .npv / .npt / .sip). RootNet reads the raw file
                 and offers Copy — it never decrypts or parses it itself. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.npvt" />
                <data android:pathPattern=".*\\.npv" />
                <data android:pathPattern=".*\\.npt" />
                <data android:pathPattern=".*\\.sip" />
            </intent-filter>
        </activity>
    </application>

</manifest>
